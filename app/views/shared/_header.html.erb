<div class="main-header">
  <%= image_tag 'logo.png', :height => '200' %>

  <div class="navi">
    <% @categories.each do |category| %>
      <%= link_to category["name"],category["link"] %>
    <% end %>
  </div>
</div>
